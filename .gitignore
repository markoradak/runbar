.DS_Store
.build/
DerivedData/
*.xcuserstate
xcuserdata/
*.profraw
*.xcresult

# Generated deterministically by bootstrap.sh from project.yml.
Runbar.xcodeproj/
# Anchored so it stays the release workflow's staging dir and doesn't also
# swallow a nested site/build/.
/build/

# Landing page (site/)
node_modules/
.next/
.vercel/
next-env.d.ts
*.tsbuildinfo

# Local tool configuration
.claude/
.codex/
.mcp.json
